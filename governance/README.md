# Governance Artefacts

All files in this directory are repository-owned, versioned, read-only foundation evidence for Gate 0B. Runtime loading is restricted to the fixed file map in `backend/src/platform/governance-loader.ts`; each document is Zod validated and recursively frozen. Updates require repository review and a new artefact version.
