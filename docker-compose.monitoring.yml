<<<<<<< HEAD
version: '3.8'

services:
  # ─────── Prometheus ───────────────────────────────────────────────────────────

  prometheus:
    image: prom/prometheus:latest
    container_name: aiarbitech-youtube-os-prometheus
=======
services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: youtube_os
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d youtube_os"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - monitoring

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.16.0
    restart: unless-stopped
    environment:
      DATA_SOURCE_NAME: postgresql://postgres:postgres@postgres:5432/youtube_os?sslmode=disable
    ports:
      - "9187:9187"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - monitoring

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - youtube_os_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - monitoring

  redis-exporter:
    image: oliver006/redis_exporter:v1.67.0
    restart: unless-stopped
    environment:
      REDIS_ADDR: redis://redis:6379
    ports:
      - "9121:9121"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - monitoring

  prometheus:
    image: prom/prometheus:v2.54.1
>>>>>>> 81fef7325c2bc9ed278736de444923623b49724f
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
<<<<<<< HEAD
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/alert-rules.yml:/etc/prometheus/alert-rules.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
    environment:
      TZ: UTC
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - monitoring

  # ─────── Grafana ──────────────────────────────────────────────────────────────

  grafana:
    image: grafana/grafana:latest
    container_name: aiarbitech-youtube-os-grafana
=======
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    networks:
      - monitoring

  grafana:
    image: grafana/grafana:11.2.2
>>>>>>> 81fef7325c2bc9ed278736de444923623b49724f
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
<<<<<<< HEAD
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/etc/grafana/dashboards:ro
    environment:
      GF_SECURITY_ADMIN_PASSWORD: '${GRAFANA_ADMIN_PASSWORD:-admin}'
      GF_SECURITY_ADMIN_USER: '${GRAFANA_ADMIN_USER:-admin}'
      GF_INSTALL_PLUGINS: 'grafana-clock-panel,grafana-simple-json-datasource'
      GF_PATHS_DASHBOARDS: /etc/grafana/dashboards
      GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH: /etc/grafana/dashboards/youtube-os-backend.json
    depends_on:
      prometheus:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - monitoring

  # ─────── Alertmanager ────────────────────────────────────────────────────────

  alertmanager:
    image: prom/alertmanager:latest
    container_name: aiarbitech-youtube-os-alertmanager
    restart: unless-stopped
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/config.yml:/etc/alertmanager/config.yml:ro
      - alertmanager_data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/config.yml'
      - '--storage.path=/alertmanager'
    environment:
      TZ: UTC
      # Alert notification credentials (set via environment)
      ALERTMANAGER_EMAIL_USERNAME: '${ALERTMANAGER_EMAIL_USERNAME:-}'
      ALERTMANAGER_EMAIL_PASSWORD: '${ALERTMANAGER_EMAIL_PASSWORD:-}'
      ALERTMANAGER_SLACK_WEBHOOK_URL: '${ALERTMANAGER_SLACK_WEBHOOK_URL:-}'
      ALERTMANAGER_PAGERDUTY_KEY: '${ALERTMANAGER_PAGERDUTY_KEY:-}'
      ALERTMANAGER_TELEGRAM_TOKEN: '${ALERTMANAGER_TELEGRAM_TOKEN:-}'
      ALERTMANAGER_TELEGRAM_CHAT_ID: '${ALERTMANAGER_TELEGRAM_CHAT_ID:-}'
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9093/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - monitoring

  # ─────── Redis Exporter ───────────────────────────────────────────────────────

  redis-exporter:
    image: oliver006/redis_exporter:latest
    container_name: aiarbitech-youtube-os-redis-exporter
    restart: unless-stopped
    ports:
      - "9121:9121"
    environment:
      REDIS_ADDR: 'redis:6379'
      REDIS_PASSWORD: '${REDIS_PASSWORD:-youtube_os}'
    depends_on:
      - redis
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9121/metrics"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - monitoring
    external_links:
      - aiarbitech-youtube-os-redis:redis

  # ─────── PostgreSQL Exporter ──────────────────────────────────────────────────

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: aiarbitech-youtube-os-postgres-exporter
    restart: unless-stopped
    ports:
      - "9187:9187"
    environment:
      DATA_SOURCE_NAME: 'postgresql://youtube_os:youtube_os@postgres:5432/youtube_os?sslmode=disable'
    depends_on:
      - postgres
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9187/metrics"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - monitoring
    external_links:
      - aiarbitech-youtube-os-postgres:postgres

  # ─────── Node Exporter ────────────────────────────────────────────────────────

  node-exporter:
    image: prom/node-exporter:latest
    container_name: aiarbitech-youtube-os-node-exporter
    restart: unless-stopped
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
      - '--collector.netdev.device-exclude=^(veth.*)$$'
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9100/metrics"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - monitoring
    pid: host
=======
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus
    networks:
      - monitoring

volumes:
  postgres_data:
  youtube_os_redis_data:
  grafana_data:
  prometheus_data:
>>>>>>> 81fef7325c2bc9ed278736de444923623b49724f

networks:
  monitoring:
    driver: bridge
<<<<<<< HEAD

volumes:
  prometheus_data:
  grafana_data:
  alertmanager_data:
=======
    name: youtube-os-monitoring
>>>>>>> 81fef7325c2bc9ed278736de444923623b49724f
