export function WorkflowStatus({ status, currentStage }: { status: string; currentStage: string }) { return <section className="card workflow-card" aria-labelledby="workflow-status-title"><p className="section-kicker">Mock execution</p><h2 id="workflow-status-title">Current workflow</h2><strong className="workflow-status">{status}</strong><p className="muted">Current stage: {currentStage}</p></section>; }
