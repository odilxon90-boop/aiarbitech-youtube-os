export function StageProgress({ progress, stage }: { progress: number; stage: string }) { return <section className="card workflow-card" aria-labelledby="workflow-progress-title"><p className="section-kicker">Mock progress</p><h2 id="workflow-progress-title">Stage progress</h2><div className="workflow-progress" aria-label={`${stage} ${progress}%`}><span style={{ width: `${progress}%` }} /></div><p className="muted">{stage} · {progress}% complete</p></section>; }
