export interface Improvement { id: string; priority: string; suggestion: string; expectedImpact: string; }
export function ImprovementList({ suggestions }: { suggestions: readonly Improvement[] }) { return <section className="card success-card success-card--wide" aria-labelledby="success-improvements-title"><p className="section-kicker">Mock AI guidance</p><h2 id="success-improvements-title">Improvements</h2><ul className="success-list">{suggestions.map((item) => <li key={item.id}><span>{item.suggestion}</span><small>{item.priority} · {item.expectedImpact}</small></li>)}</ul></section>; }
