export interface ScoreHistoryPoint { date: string; score: number; }
export function HistoryChart({ points }: { points: readonly ScoreHistoryPoint[] }) { const values = points.map((point, index) => `${index * 10},${100 - point.score}`).join(' '); return <section className="card success-card" aria-labelledby="success-history-title"><p className="section-kicker">Last 30 days</p><h2 id="success-history-title">Score history</h2><svg className="success-chart" viewBox="0 0 300 100" role="img" aria-label="30 day score history"><polyline points={values} fill="none" stroke="currentColor" strokeWidth="3" /></svg></section>; }
