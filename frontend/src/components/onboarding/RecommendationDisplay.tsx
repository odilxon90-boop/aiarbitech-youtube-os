export function RecommendationDisplay({ recommendations }: { recommendations: readonly string[] }) { return <section className="card onboarding-card" aria-labelledby="recommendations-title"><p className="section-kicker">Mock AI guidance</p><h2 id="recommendations-title">Recommendations</h2><ul className="onboarding-recommendations">{recommendations.map((item) => <li key={item}>{item}</li>)}</ul></section>; }
