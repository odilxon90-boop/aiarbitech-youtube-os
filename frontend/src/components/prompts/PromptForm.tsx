import { useState } from 'react';
export function PromptForm() { const [saved, setSaved] = useState(false); return <section className="card prompt-card" aria-labelledby="prompt-form-title"><p className="section-kicker">Mock CRUD</p><h2 id="prompt-form-title">Create prompt</h2><form onSubmit={(event) => { event.preventDefault(); setSaved(true); }}><label>Name<input aria-label="Prompt name" defaultValue="New mock prompt" /></label><label>Content<textarea aria-label="Prompt content" defaultValue="Write a mock prompt." /></label><button type="submit">Save mock prompt</button>{saved && <p role="status">Mock prompt saved.</p>}</form></section>; }
