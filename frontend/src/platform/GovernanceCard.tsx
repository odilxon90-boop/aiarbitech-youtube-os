import type { GovernanceArtifact } from './types';
interface Props { title:string; artifact:GovernanceArtifact; summary:string; highlights:readonly string[] }
function display(value:unknown):string { if(typeof value==='string'||typeof value==='number'||typeof value==='boolean')return String(value); if(Array.isArray(value))return `${value.length} declared`; return value&&typeof value==='object'?'DECLARED':'NOT_VERIFIED'; }
export function GovernanceCard({title,artifact,summary,highlights}:Props){return <section className="card governance-card"><p className="section-kicker">{artifact.artifactType}</p><h2>{title}</h2><p className="lead">{summary}</p><dl className="compact-grid">{highlights.map((key)=><div key={key}><dt>{key.replace(/([A-Z])/g,' $1')}</dt><dd>{display(artifact[key])}</dd></div>)}</dl>{artifact.evidence&&<div className="evidence-strip"><strong>{artifact.evidence.status}</strong><span>{artifact.evidence.confidence} confidence</span><span>{artifact.evidence.decisionClassification}</span></div>}</section>}
