import type { CapabilityRecord, CapabilityRegistry } from './types';

function values(items: readonly string[]): string { return items.length === 0 ? 'NONE' : items.join(', '); }
function CapabilityRow({ capability }: { capability: CapabilityRecord }) { return <article className="capability-record"><header><div><p className="section-kicker">{capability.capabilityId}</p><h3>{capability.capabilityName}</h3></div><strong>{capability.implementationStatus}</strong></header><p className="muted">{capability.capabilityDescription}</p><dl className="compact-grid"><div><dt>Category</dt><dd>{capability.capabilityCategory}</dd></div><div><dt>Current Status</dt><dd>{capability.currentStatus}</dd></div><div><dt>Lifecycle Status</dt><dd>{capability.lifecycleStatus}</dd></div><div><dt>Evidence Status</dt><dd>{capability.evidenceStatus}</dd></div><div><dt>Confidence Level</dt><dd>{capability.confidenceLevel}</dd></div><div><dt>Certification Status</dt><dd>{capability.certificationStatus}</dd></div><div><dt>Required Global Services</dt><dd>{values(capability.requiredGlobalServices)}</dd></div><div><dt>Dependencies</dt><dd>{values(capability.dependencies)}</dd></div><div><dt>Required Contracts</dt><dd>{values(capability.requiredContracts)}</dd></div></dl></article> }
export function CapabilityRegistryPage({ registry }: { registry: CapabilityRegistry }) { return <section className="card card--capabilities"><p className="section-kicker">Read-only Governance Metadata</p><h2>Platform Capability Registry</h2><p className="lead">The existing repository-owned registry is the single authoritative local source for capability status. No capability can be edited, activated, or executed here.</p>{registry.capabilities.length === 0?<p className="muted">No capabilities are registered.</p>:<div className="capability-records">{registry.capabilities.map((capability)=><CapabilityRow key={capability.capabilityId} capability={capability}/>)}</div>}</section> }