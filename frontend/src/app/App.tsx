import { useEffect,useMemo,useState } from 'react';import { AppShell } from './AppShell';import { createPlatformFoundationClient,type PlatformFoundationClient } from '../platform/platform-client';import type { PlatformFoundationStatus } from '../platform/types';import { GlobalEcosystemStatusPage } from '../platform/GlobalEcosystemStatusPage';import { HealthStatusPage } from '../platform/HealthStatusPage';import { PlatformIdentityPage } from '../platform/PlatformIdentityPage';import { PlatformRegistrationPage } from '../platform/PlatformRegistrationPage';import { Gate0BDashboard } from '../platform/Gate0BDashboard';import { EmptyState,ErrorState,LoadingState } from '../shared/components/AsyncStates';import { errorState,loadingState,successState,type AsyncState } from '../shared/async-state';
export interface AppProps{client?:PlatformFoundationClient}
export function App({client}:AppProps){const platformClient=useMemo(()=>client??createPlatformFoundationClient(),[client]);const[state,setState]=useState<AsyncState<PlatformFoundationStatus>>(loadingState());useEffect(()=>{const controller=new AbortController();setState(loadingState());platformClient.loadFoundationStatus(controller.signal).then((status)=>setState(successState(status))).catch((error:unknown)=>{if(!controller.signal.aborted)setState(errorState(error instanceof Error?error.message:'Unknown platform status error'))});return()=>controller.abort()},[platformClient]);return <AppShell>{state.status==='loading'&&<LoadingState/>}{state.status==='error'&&<ErrorState message={state.error??'Unknown error'}/>} {state.status==='empty'&&<EmptyState/>}{state.status==='success'&&state.data&&<div className="dashboard-grid"><PlatformRegistrationPage registration={state.data.registration}/><PlatformIdentityPage manifest={state.data.manifest}/><HealthStatusPage health={state.data.health}/><GlobalEcosystemStatusPage status={{...state.data.connection,capabilities:state.data.manifest.globalEcosystemCompatibility.capabilities}}/><Gate0BDashboard governance={state.data.governance}/></div>}</AppShell>}
